import React, {useState, useEffect} from 'react';
import { useNavigate, useLocation } from "react-router-dom";

function App() {
  return (
      <Router>
        <Routes>
          <Route
          />
        </Routes>
      </Router>
  );
}

export default App;
